# Tasks: Refine Port Selection Strategy

1.  **修改 Server 启动逻辑**
    - [x] 更新 `src/server.rs` 中的 `start` 函数。
    - [x] 实现“先试默认端口，再随机”的逻辑。
    - [x] 验证：编译无误。

2.  **功能验证**
    - [x] 启动一个不带端口参数的实例，确认其监听在 `9568`。
    - [x] 在第一个实例运行的同时，启动第二个不带端口参数的实例，确认其监听在一个随机的高位端口。
    - [x] 验证：TUI 仪表盘显示正确的实际端口。

3.  **单元测试 (可选)**
    - [x] 如果可能，增加针对该逻辑分支的测试。
