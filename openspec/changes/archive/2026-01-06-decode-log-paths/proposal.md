# Change: 日志路径 URL 解码优化

## Why
目前的日志记录直接获取请求的原始路径，这会导致包含空格、中文或特殊字符的路径以 URL 编码形式（如 `%20`）展示，降低了 TUI 仪表盘的可读性。

## What Changes
- 修改 `src/logger.rs` 中的日志捕获逻辑。
- 使用 `percent-encoding` 对请求路径进行解码。
- 确保存储在 `LogEntry` 中的路径是人类可读的原始字符。

## Impact
- **可读性**: 日志中的路径显示将与文件系统中的实际名称完全一致。
