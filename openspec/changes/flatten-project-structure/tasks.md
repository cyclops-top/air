# Tasks: Flatten Project Structure

1.  **执行文件移动**
    - [x] 移动 `air/` 下的所有可见和隐藏文件到根目录。
    - [x] 移除 `air/` 目录。
    - [x] 验证：根目录下存在 `Cargo.toml`, `Makefile`, `src/` 等。

2.  **验证构建与运行**
    - [x] 在根目录运行 `make build`。
    - [x] 在根目录运行 `make test`。
    - [x] 验证：程序能正常编译并运行。

3.  **更新项目文档**
    - [x] 检查 `openspec/project.md` 是否需要更新。
    - [x] 验证：文档描述与实际结构一致。
