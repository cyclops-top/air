# Tasks: Implement Cyber-Space Glassmorphism UI

- [x] **Resource Integration**
    - Add Tailwind-style CSS (inlined), Google Fonts equivalent (system fonts), and Material Symbols equivalent (SVGs) to `src/view.rs`.
- [x] **CSS & Layout Overhaul**
    - Implement custom CSS classes (glassmorphism, glow-cyan, pulse-green, etc.) in `src/view.rs`.
    - Update the main layout structure to match the "Enhanced Clarity" design.
- [x] **Navigation & Header Implementation**
    - Implement the new Header with "System Online" indicator and IP display.
    - Update `render_breadcrumbs` to support the new styled breadcrumbs and 'hub' icon.
- [x] **File Listing Redesign**
    - Rewrite the file listing loop to generate the new table structure.
    - Implement file-type detection for dynamic icon selection (folders vs different file extensions).
    - Format modification times to match the `YYYY.MM.DD // HH:MM` design.
- [x] **Refinement & Polish**
    - Ensure responsive behavior on different screen sizes.
    - Verify all links and download buttons function correctly with the new structure.
- [x] **Verification**
    - [x] Verify the UI renders correctly in a browser.
    - [x] Verify breadcrumbs navigation works.
    - [x] Verify file downloads work.
    - [x] Verify the layout is responsive.
