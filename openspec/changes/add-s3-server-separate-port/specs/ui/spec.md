# UI Requirements

## MODIFIED Requirements

### Requirement: 全屏 TUI 仪表盘
TUI 仪表盘 MUST 同时监控并显示 Web 和 S3 服务的状态。

#### Scenario: 仪表盘地址展示
仪表盘顶部 MUST 同时列出：
- Web UI 的 Network/Host 访问地址。
- S3 API 的 Network/Host 访问地址。
